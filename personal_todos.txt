for this backend we need to do 3 basic things

1. send and receive data from db 
[x] send data to frontend (from db or from gpt)
[ ] receive data from front-end to put into DB or GPT 
[ ] send/receive data from GPT api call

2. send and receive data to GPT 
[ ] how to structure the data to send and recieve 

3. maybe do some processing
[ ]


Accessibilty
[ ] - welcome message in speech
[ ] - read out questions and take answers


frontend example POST
```
const addPerson = async (person) => {
  const response = await fetch('/api/people', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
    },
    body: JSON.stringify(person),
  });
  if (!response.ok) {
    throw new Error('Failed to add person');
  }
  return await response.json();
};

// Example usage
addPerson({ name: 'Jane Doe', age: 32 })
  .then(person => console.log('Added person:', person))
  .catch(error => console.error('Error adding person:', error));
```

frontend example GET 